<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <% if user_signed_in?  %>
          <a class="navbar-brand logo-ff" href="<%= activities_path %>">Friend<span>Face</span></a>
      <% else %>
          <a class="navbar-brand logo-ff" href="<%= root_path %>">Friend<span>Face</span></a>
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'All statuses', feed_path %></li>
        <% if user_signed_in?  %>
        <li><%= link_to 'Activity', activities_path %></li>
        <% end %>
      </ul>
      <%= form_tag(statuses_path, :method => 'get', :id => 'search-form', :role => 'search', :class => 'navbar-form navbar-left') do %>
          <div class="form-group" >
            <%= text_field_tag :search, params[:search], placeholder: "Find status", class: 'form-control' %>
          </div>
          <%= submit_tag "Search", class: 'btn btn-success btn-custom-transparent' %>
      <% end %>
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
            <li><%= link_to 'Friends', user_friendships_path %></li>
            <li><%= link_to 'Galleries', albums_path(current_user) %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <li><%= link_to current_user.full_name, edit_user_registration_path %></li>
            <li><%= link_to 'Log out', logout_path %></li>
        <% else %>
            <li><%= link_to  'Sign up', register_path %></li>
            <li><%= link_to  'Sign in', login_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>